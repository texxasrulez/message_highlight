# Changelog

All notable changes to `message_highlight` should be documented in this file.

## [Unreleased]

- Ongoing development builds use `message_highlight::PLUGIN_VERSION` with a `+dev` suffix until the next release is cut.

## [1.0.2] - 2026-04-11

- Formalized the plugin's self-metadata through `message_highlight::PLUGIN_VERSION` and `message_highlight::info()`.
- Aligned self-versioning with a cleaner release workflow while keeping existing plugin behavior intact.
